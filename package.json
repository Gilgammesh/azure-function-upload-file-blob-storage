{
  "name": "upload-file-to-blob-storage",
  "version": "1.0.0",
  "description": "Azure Function - Upload file to Blob Storage",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "start-azurite": "azurite --silent --location azureStorage --debug azureStorage/debug.log"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.12.0",
    "@types/parse-multipart": "^1.0.0",
    "azurite": "^3.20.1",
    "http-status-enum": "^1.0.2",
    "parse-multipart": "^1.0.4"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "16.x",
    "typescript": "^4.0.0"
  }
}
